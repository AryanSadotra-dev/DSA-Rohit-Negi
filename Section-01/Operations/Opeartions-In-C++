/*
====================================================================
📘 LECTURE NOTES
OPERATORS IN C++
(Arithmetic, Unary, Comparison, Logical, Bitwise, Assignment)
180 Days Coding Challenge – Day 12

⚠️ These notes are a DIRECT classroom reconstruction
⚠️ Hinglish language is intentional (teacher style)
⚠️ Nothing is skipped
⚠️ Repetition is intentional (sir style)
⚠️ Written as ONE GitHub-ready file
====================================================================
*/

#include <iostream>
using namespace std;

int main()
{
    /*
    ============================================================
    1️⃣ OPENING – CLASSROOM START
    ============================================================

    "Kaise hain aap sab log?"

    "Aaj hum ek bahut IMPORTANT topic discuss
     karne wale hain – OPERATORS."

    "12th day aa chuka hai challenge ka,
     aur aap log bahut ache se step-by-step
     aage badh rahe ho."

    "Ye topic aapne 10th, 11th, 12th me
     padha hoga…"

    "Agar padha hota to aaj idhar coding
     kar rahe hote 😄"

    ============================================================
    */


    /*
    ============================================================
    2️⃣ WHAT IS AN OPERATOR?
    ============================================================

    Sir explains:

    "Operator ka kaam hota hai OPERATION karna."

    Example:
    +   → addition
    -   → subtraction
    *   → multiplication
    /   → division

    "Operator khud kuch nahi karta,
     operands pe kaam karta hai."

    ============================================================
    */


    /*
    ============================================================
    3️⃣ ARITHMETIC OPERATORS
    ============================================================

    +  -  *  /  %

    Example:
    6 + 2 = 8
    6 - 2 = 4
    6 * 2 = 12
    6 / 2 = 3
    7 % 2 = 1

    ============================================================
    */

    int a = 13;
    int b = 2;

    cout << "13 / 2 = " << (a / b) << endl;   // 6
    cout << "13 % 2 = " << (a % b) << endl;   // 1


    /*
    ============================================================
    4️⃣ TYPE RULE IN DIVISION
    ============================================================

    Sir says:

    "Rule yaad rakho."

    👉 Agar DONO operands int hain →
       answer int hoga

    👉 Agar EK bhi float/double hai →
       answer float/double hoga

    ============================================================
    */

    cout << "13.0 / 2 = " << (13.0 / 2) << endl;   // 6.5
    cout << "13 / 2.0 = " << (13 / 2.0) << endl;   // 6.5


    /*
    ============================================================
    5️⃣ OPERATOR PRECEDENCE & ASSOCIATIVITY
    ============================================================

    Precedence (high → low):

    *  /  %
    +  -

    Associativity:
    Same precedence → Left to Right

    Example:
    2 * 3 + 4 = 10
    3 + 4 - 6 = 1

    ============================================================
    */

    cout << "2 * 3 + 4 = " << (2 * 3 + 4) << endl;
    cout << "3 + 4 - 6 = " << (3 + 4 - 6) << endl;


    /*
    ============================================================
    6️⃣ UNARY OPERATORS (++ , --)
    ============================================================

    ++a  → Pre-increment
    a++  → Post-increment

    Sir explains:

    "Post me pehle kaam hota hai,
     baad me value badhti hai."

    "Pre me pehle value badhti hai,
     baad me kaam hota hai."

    ============================================================
    */

    int x = 10;

    cout << "Post Increment: " << x++ << endl; // 10
    cout << "After Post Increment: " << x << endl; // 11

    x = 10;
    cout << "Pre Increment: " << ++x << endl; // 11


    /*
    ============================================================
    7️⃣ DECREMENT OPERATORS
    ============================================================

    --a  → Pre-decrement
    a--  → Post-decrement

    ============================================================
    */

    int y = 10;
    cout << "Post Decrement: " << y-- << endl; // 10
    cout << "After Post Decrement: " << y << endl; // 9


    /*
    ============================================================
    8️⃣ COMPARISON OPERATORS
    ============================================================

    ==   Equal to
    !=   Not equal
    >    Greater than
    <    Less than
    >=   Greater or equal
    <=   Less or equal

    IMPORTANT RULE:
    👉 Output hamesha 0 ya 1 hota hai

    ============================================================
    */

    int p = 4, q = 5;

    cout << (p == q) << endl;   // 0
    cout << (p != q) << endl;   // 1
    cout << (p < q) << endl;    // 1
    cout << (p > q) << endl;    // 0


    /*
    ============================================================
    9️⃣ COMPARISON IN IF-ELSE
    ============================================================
    */

    if (p < q)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;


    /*
    ============================================================
    🔟 LOGICAL OPERATORS
    ============================================================

    &&  → AND
    ||  → OR
    !   → NOT

    AND:
    true && true → true
    else → false

    OR:
    true || false → true

    NOT:
    !true → false

    ============================================================
    */

    int age = 25;

    if (age > 18 && age < 60)
        cout << "Eligible" << endl;
    else
        cout << "Not Eligible" << endl;


    /*
    ============================================================
    1️⃣1️⃣ SHORT-CIRCUIT BEHAVIOR
    ============================================================

    AND:
    Agar pehla false → baaki check nahi

    OR:
    Agar pehla true → baaki check nahi

    Computer lazy hota hai 😄
    Jahan answer mil gaya, wahi ruk jata hai

    ============================================================
    */


    /*
    ============================================================
    1️⃣2️⃣ VOWEL / CONSONANT USING LOGICAL OR
    ============================================================
    */

    char ch = 'a';

    if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u')
        cout << "Vowel" << endl;
    else
        cout << "Consonant" << endl;


    /*
    ============================================================
    1️⃣3️⃣ BITWISE OPERATORS
    ============================================================

    &   AND
    |   OR
    ^   XOR
    ~   Complement
    <<  Left shift
    >>  Right shift

    Work at BIT LEVEL

    ============================================================
    */

    cout << "2 & 3 = " << (2 & 3) << endl; // 2
    cout << "2 | 3 = " << (2 | 3) << endl; // 3
    cout << "2 ^ 3 = " << (2 ^ 3) << endl; // 1


    /*
    ============================================================
    1️⃣4️⃣ LEFT & RIGHT SHIFT
    ============================================================

    Left Shift → multiply by 2
    Right Shift → divide by 2

    ============================================================
    */

    cout << "3 << 1 = " << (3 << 1) << endl; // 6
    cout << "6 >> 1 = " << (6 >> 1) << endl; // 3


    /*
    ============================================================
    1️⃣5️⃣ ASSIGNMENT OPERATORS
    ============================================================

    =   +=   -=   *=   /=   %=

    a += 5 → a = a + 5

    ============================================================
    */

    int k = 10;
    k += 5;
    cout << k << endl; // 15

    k -= 3;
    cout << k << endl; // 12


    /*
    ============================================================
    1️⃣6️⃣ FINAL SIR MESSAGE
    ============================================================

    "Operator ratne ki cheez nahi hai."

    "Logic samajh aaya →
     coding easy ho jaati hai."

    "Precedence aur associativity
     computer ko confuse hone se bachati hai."

    "Next lecture me:
     loops + questions."

    "Jai Hind 🇮🇳 Jai Bharat 🇮🇳"

    ============================================================
    */

    return 0;
}

/*
====================================================================
END OF LECTURE – OPERATORS IN C++
====================================================================
*/
